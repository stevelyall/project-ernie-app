machine:
  node:
    version:
      0.10.28

test:
  pre:
    - npm install
    - npm install -g cordova
    - npm install -g ionic
    - ls
    - ionic platform add android
    - android update sdk --no-ui
    - ionic build android
    - sleep 5
    - y
#    - npm start:
#        background: true
#    - sleep 5
  override:
#    - npm run test-single-run
#    - npm run protractor