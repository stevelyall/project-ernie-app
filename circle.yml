machine:
  node:
    version:
      0.10.28

test:
  pre:
    - npm install
    - npm install -g cordova
    - npm install -g ionic
    - cd project-ernie-app
    -ls -al
    - npm start:
        background: true
    - sleep 5
  override:
    - npm run test-single-run
    - npm run protractor