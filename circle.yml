machine:
  node:
    version:
      0.10.28

test:
  pre:
    - npm install
    - npm install -g cordova
    - npm install -g ionic
    - npm install -g protractor
    - npm start:
        background: true
    - sleep 5
  override:
#    - npm run test-single-run
    - cd test
#   - web-driver update
    - ionic serve --address localhost
    - npm run protractor